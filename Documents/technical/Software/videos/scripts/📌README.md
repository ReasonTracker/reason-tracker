# 📌 Scripts

`load-episode-definition.mjs` is the Remotion webpack loader for both `episode.json` and `episode.ts`. It normalizes episode modules and supplies image aspect-ratio metadata for files in each episode's adjacent `media` directory.

<!-- autonav:start -->
<!-- autonav:end -->
